import type { Metadata } from 'next';

export const metadata: Metadata = { title: '建立問卷' };

export default function NewSurveyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
