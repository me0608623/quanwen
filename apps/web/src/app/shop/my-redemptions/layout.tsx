import type { Metadata } from 'next';

export const metadata: Metadata = { title: '我的兌換' };

export default function MyRedemptionsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
