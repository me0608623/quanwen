import type { Metadata } from 'next';

export const metadata: Metadata = { title: '申訴管理' };

export default function AdminAppealsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
