import type { Metadata } from 'next';

export const metadata: Metadata = { title: '提現審核' };

export default function AdminWithdrawalsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
